# Stage 1: Node build
FROM node:22-alpine AS build
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci
COPY . .
RUN npm run build

# Stage 2: Nginx serve
FROM nginx:1.27-alpine
COPY --from=build /app/dist /usr/share/nginx/html/cms
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
